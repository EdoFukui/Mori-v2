// Mock de imports de CSS para Jest (ej. `import './admin.css'`).
// Estos imports son solo efectos secundarios (cargar la hoja de
// estilo en el navegador vía Vite) — en test no hay nada que
// verificar de su contenido, así que se devuelve un objeto vacío.
module.exports = {};
